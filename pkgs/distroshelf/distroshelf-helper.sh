#!/usr/bin/env bash
# distroshelf-helper — host-side integration for the DistroShelf flatpak/app.
# Imported functionally from Bazzite (which ships it as a plain file); adjust
# to the upstream helper contract.
set -euo pipefail
echo "distroshelf-helper: see github.com/ranfdev/DistroShelf for the helper contract"
