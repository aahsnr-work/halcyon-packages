# copr-build — build halcyon-packages on Fedora Copr.
#
# Replaces anda-build.yml + anda-publish.yml (the anda/mock/Pages era). Copr
# now owns the build farm, the signing (per-project key) and the repo serving:
#
#   manifest   ci/matrix.py selects packages from the git push (changed dir +
#              every higher batch) and emits one build_matrix per batch
#   submitN    spectool -g + rpmbuild -bs per package (the specs carry full
#              Source URLs — the old anda SRPM-fetch step, now explicit) and
#              `copr-cli build --nowait halcyon <srpm>`; the build id lands in
#              an artifact
#   waitN      `copr-cli watch-build` over the wave's ids, then a status
#              re-check so a failed build fails the run. Copr makes each
#              successful build visible to the project repo immediately, so
#              wave N+1's buildroot sees wave N's output — the same guarantee
#              the Pages publish used to provide, without the publish step.
#
# PRs run the validate job only (no Copr builds — checks are syntax + registry
# level; a failed Copr build leaves the published repo on the last good
# version, so main-line failures are self-healing).
#
# External repos (per-chroot, copr-cli edit-chroot): the lionheartp/Hyprland
# Copr as a lowest-priority bootstrap for fresh projects, and Terra 44 (it
# supplies anda-srpm-macros, which Fedora 44 does not carry).
name: copr-build

on:
  push:
    branches: [main]
    paths: ["pkgs/**", "ci/**", ".github/builder/**", ".github/workflows/copr-build.yml"]
  pull_request:
    paths: ["pkgs/**", "ci/**", ".github/builder/**", ".github/workflows/copr-build.yml"]
  workflow_dispatch:
    inputs:
      only:
        description: "Packages to build (space separated); empty = all"
        required: false
        default: ""

permissions:
  contents: read

concurrency:
  group: copr-build
  cancel-in-progress: false

env:
  COPR_PROJECT: halcyon

jobs:
  validate:
    name: Validate the registry
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - name: Wait for the builder image of this commit
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -eu
          # builder-docker rebuilds the image on the same pushes that trigger
          # this workflow (.github/builder/**); without this gate the submit
          # jobs race it and some pull the stale image. When no builder-image
          # run exists for this SHA the image inputs did not change and there
          # is nothing to wait for.
          for i in $(seq 1 60); do
            run_id=$(gh api "repos/$GITHUB_REPOSITORY/actions/workflows/builder-docker.yml/runs?head_sha=${{ github.sha }}&per_page=1" \
              --jq '.workflow_runs[0].id // empty')
            if [ -z "$run_id" ]; then
              echo "no builder-image run for ${{ github.sha }} — image unchanged"
              exit 0
            fi
            read -r status conclusion <<<"$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$run_id" --jq '.status + " " + (.conclusion // "-")')"
            if [ "$status" = completed ]; then
              [ "$conclusion" = success ] || { echo "builder-image run $run_id: $conclusion"; exit 1; }
              echo "builder-image run $run_id is green"
              exit 0
            fi
            echo "builder-image run $run_id: $status; waiting ($i/60)"
            sleep 20
          done
          echo "timed out waiting for the builder image"; exit 1
      - name: Shell syntax + lint (package helper scripts)
        run: |
          set -eu
          # bash -n, not sh -n: every script here is bash (arrays, process
          # substitution), and /bin/sh is dash on the runners
          for f in pkgs/*/*.sh; do
            [ -e "$f" ] || continue
            bash -n "$f"
          done
          # the shellcheck binary ships preinstalled on the runners
          shellcheck -S warning pkgs/*/*.sh
      - name: Python syntax
        run: python3 -m py_compile ci/*.py ci/sweep/*.py
      - name: Registry consistency + build plan
        run: |
          set -eu
          python3 ci/matrix.py --list
          BEFORE="${{ github.event.before }}"
          if [ -n "$BEFORE" ] && [ "$BEFORE" != "0000000000000000000000000000000000000000" ] \
             && git cat-file -e "$BEFORE^{commit}" 2>/dev/null; then
            python3 ci/matrix.py --list --since "$BEFORE"
          fi
      - name: Lint the workflows (actionlint + shellcheck)
        run: |
          set -euo pipefail
          # pinned release + checksum: no curl-to-bash, no floating tags
          version=1.7.12
          checksum=8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8
          curl -fsSL -o /tmp/actionlint.tar.gz \
            "https://github.com/rhysd/actionlint/releases/download/v${version}/actionlint_${version}_linux_amd64.tar.gz"
          echo "${checksum}  /tmp/actionlint.tar.gz" | sha256sum -c -
          tar -xzf /tmp/actionlint.tar.gz -C /tmp actionlint
          /tmp/actionlint -color

  manifest:
    name: Build matrix (per batch)
    needs: validate
    # PRs stop here: validation only, no Copr builds
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      batches: ${{ steps.split.outputs.batches }}
      batch0: ${{ steps.split.outputs.batch0 }}
      batch1: ${{ steps.split.outputs.batch1 }}
      batch2: ${{ steps.split.outputs.batch2 }}
      batch3: ${{ steps.split.outputs.batch3 }}
      batch4: ${{ steps.split.outputs.batch4 }}
      batch5: ${{ steps.split.outputs.batch5 }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - name: Resolve the selection
        id: selection
        env:
          ONLY: ${{ inputs.only }}
          EVENT: ${{ github.event_name }}
          BEFORE: ${{ github.event.before }}
        run: |
          set -eu
          if [ -n "${ONLY:-}" ]; then
            echo "args=--only ${ONLY}" >> "$GITHUB_OUTPUT"
          elif [ "$EVENT" = push ] && [ -n "${BEFORE:-}" ] \
               && [ "$BEFORE" != "0000000000000000000000000000000000000000" ] \
               && git cat-file -e "${BEFORE}^{commit}" 2>/dev/null; then
            echo "args=--since ${BEFORE}" >> "$GITHUB_OUTPUT"
          else
            echo "args=" >> "$GITHUB_OUTPUT"
          fi
      - name: Split the matrix into batch waves
        id: split
        env:
          SELECT_ARGS: ${{ steps.selection.outputs.args }}
        run: |
          set -eu
          # Word splitting on SELECT_ARGS is intended — it is a flag list.
          # shellcheck disable=SC2086
          python3 ci/matrix.py $SELECT_ARGS > /tmp/matrix.out
          # the matrix output is read from a file: feeding it through a pipe
          # alongside the heredoc below would make the heredoc override the
          # pipe and the parser would see nothing
          python3 - "$GITHUB_OUTPUT" /tmp/matrix.out <<'PY'
          import json, sys
          out = sys.argv[1]
          matrix, batches = [], []
          for line in open(sys.argv[2]):
              if line.startswith("build_matrix="):
                  matrix = json.loads(line[len("build_matrix="):].strip())
              elif line.startswith("batches="):
                  batches = line[len("batches="):].strip().split(",")
          if not matrix:
              print("no entries")
          def with_output(name, value):
              with open(f"{out}", "a") as fh:
                  fh.write(f"{name}={value}\n")
          with_output("batches", ",".join(batches))
          for b in batches:
              entries = [e for e in matrix if e["labels"]["batch"] == b]
              with_output(f"batch{b}", json.dumps(entries, sort_keys=True))
              print(f"batch {b}: {len(entries)} package(s)")
          PY

  submit0:
    name: "submit batch 0"
    needs: manifest
    if: needs.manifest.outputs.batch0 != ''
    strategy:
      matrix:
        pkg: ${{ fromJson(needs.manifest.outputs.batch0) }}
      fail-fast: false
    runs-on: ubuntu-latest
    timeout-minutes: 45
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Workspace safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Build the SRPM and submit to Copr
        env:
          NAME: ${{ matrix.pkg.name }}
        run: |
          set -euo pipefail
          spec="pkgs/$NAME/$NAME.spec"
          mkdir -p "_srpms/$NAME"
          spectool -g -C "_srpms/$NAME" "$spec"
          # spectool fetches only URL sources; local (non-URL) Source/Patch
          # entries (onlyoffice's metainfo, hyprland's macros.hyprland, …)
          # live in the package dir and go into the sourcedir verbatim
          cp -a "pkgs/$NAME/." "_srpms/$NAME/"
          srpm=$(rpmbuild -bs \
              --define "_sourcedir $PWD/_srpms/$NAME" \
              --define "_srcrpmdir $PWD/_srpms" \
              --define "_specdir $PWD/$(dirname "$spec")" \
              "$spec" | sed -n 's/^Wrote: //p')
          [ -n "$srpm" ] || { echo "rpmbuild -bs produced no SRPM"; exit 1; }
          copr-cli build --nowait "$COPR_PROJECT" "$srpm" | tee submit.log
          id=$(sed -n 's/^Created builds: //p' submit.log)
          [ -n "$id" ] || { echo "no build id in copr-cli output"; exit 1; }
          echo "$id" > "build-id-$NAME.txt"
      - name: Upload the build id
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: "submit-0-${{ matrix.pkg.name }}"
          path: build-id-*.txt
          if-no-files-found: error
          retention-days: 3

  wait0:
    name: "wait for batch 0"
    needs: [manifest, submit0]
    if: always() && needs.manifest.outputs.batch0 != '' && needs.submit0.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 330
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Download the wave's build ids
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: "submit-0-*"
          merge-multiple: true
          path: ids
      - name: Watch the wave's builds
        run: |
          set -eu
          ids=$(cat ids/build-id-*.txt | tr '\n' ' ')
          echo "watching builds: $ids"
          # the ids are one space-joined list
          # shellcheck disable=SC2086
          copr-cli watch-build $ids
          # watch-build's exit code is not a contract; re-check every id so a
          # failed build fails this job no matter what
          rc=0
          # shellcheck disable=SC2086
          for id in $ids; do
            status=$(copr-cli status "$id")
            if [ "$status" != succeeded ]; then
              echo "build $id: $status"
              rc=1
            fi
          done
          exit $rc

  submit1:
    name: "submit batch 1"
    needs: [manifest, wait0]
    if: always() && needs.manifest.outputs.batch1 != '' && (needs.wait0.result == 'success' || needs.wait0.result == 'skipped')
    strategy:
      matrix:
        pkg: ${{ fromJson(needs.manifest.outputs.batch1) }}
      fail-fast: false
    runs-on: ubuntu-latest
    timeout-minutes: 45
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Workspace safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Build the SRPM and submit to Copr
        env:
          NAME: ${{ matrix.pkg.name }}
        run: |
          set -euo pipefail
          spec="pkgs/$NAME/$NAME.spec"
          mkdir -p "_srpms/$NAME"
          spectool -g -C "_srpms/$NAME" "$spec"
          # spectool fetches only URL sources; local (non-URL) Source/Patch
          # entries (onlyoffice's metainfo, hyprland's macros.hyprland, …)
          # live in the package dir and go into the sourcedir verbatim
          cp -a "pkgs/$NAME/." "_srpms/$NAME/"
          srpm=$(rpmbuild -bs \
              --define "_sourcedir $PWD/_srpms/$NAME" \
              --define "_srcrpmdir $PWD/_srpms" \
              --define "_specdir $PWD/$(dirname "$spec")" \
              "$spec" | sed -n 's/^Wrote: //p')
          [ -n "$srpm" ] || { echo "rpmbuild -bs produced no SRPM"; exit 1; }
          copr-cli build --nowait "$COPR_PROJECT" "$srpm" | tee submit.log
          id=$(sed -n 's/^Created builds: //p' submit.log)
          [ -n "$id" ] || { echo "no build id in copr-cli output"; exit 1; }
          echo "$id" > "build-id-$NAME.txt"
      - name: Upload the build id
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: "submit-1-${{ matrix.pkg.name }}"
          path: build-id-*.txt
          if-no-files-found: error
          retention-days: 3

  wait1:
    name: "wait for batch 1"
    needs: [manifest, submit1]
    if: always() && needs.manifest.outputs.batch1 != '' && needs.submit1.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 330
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Download the wave's build ids
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: "submit-1-*"
          merge-multiple: true
          path: ids
      - name: Watch the wave's builds
        run: |
          set -eu
          ids=$(cat ids/build-id-*.txt | tr '\n' ' ')
          echo "watching builds: $ids"
          # the ids are one space-joined list
          # shellcheck disable=SC2086
          copr-cli watch-build $ids
          rc=0
          # shellcheck disable=SC2086
          for id in $ids; do
            status=$(copr-cli status "$id")
            if [ "$status" != succeeded ]; then
              echo "build $id: $status"
              rc=1
            fi
          done
          exit $rc

  submit2:
    name: "submit batch 2"
    needs: [manifest, wait1]
    if: always() && needs.manifest.outputs.batch2 != '' && (needs.wait1.result == 'success' || needs.wait1.result == 'skipped')
    strategy:
      matrix:
        pkg: ${{ fromJson(needs.manifest.outputs.batch2) }}
      fail-fast: false
    runs-on: ubuntu-latest
    timeout-minutes: 45
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Workspace safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Build the SRPM and submit to Copr
        env:
          NAME: ${{ matrix.pkg.name }}
        run: |
          set -euo pipefail
          spec="pkgs/$NAME/$NAME.spec"
          mkdir -p "_srpms/$NAME"
          spectool -g -C "_srpms/$NAME" "$spec"
          # spectool fetches only URL sources; local (non-URL) Source/Patch
          # entries (onlyoffice's metainfo, hyprland's macros.hyprland, …)
          # live in the package dir and go into the sourcedir verbatim
          cp -a "pkgs/$NAME/." "_srpms/$NAME/"
          srpm=$(rpmbuild -bs \
              --define "_sourcedir $PWD/_srpms/$NAME" \
              --define "_srcrpmdir $PWD/_srpms" \
              --define "_specdir $PWD/$(dirname "$spec")" \
              "$spec" | sed -n 's/^Wrote: //p')
          [ -n "$srpm" ] || { echo "rpmbuild -bs produced no SRPM"; exit 1; }
          copr-cli build --nowait "$COPR_PROJECT" "$srpm" | tee submit.log
          id=$(sed -n 's/^Created builds: //p' submit.log)
          [ -n "$id" ] || { echo "no build id in copr-cli output"; exit 1; }
          echo "$id" > "build-id-$NAME.txt"
      - name: Upload the build id
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: "submit-2-${{ matrix.pkg.name }}"
          path: build-id-*.txt
          if-no-files-found: error
          retention-days: 3

  wait2:
    name: "wait for batch 2"
    needs: [manifest, submit2]
    if: always() && needs.manifest.outputs.batch2 != '' && needs.submit2.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 330
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Download the wave's build ids
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: "submit-2-*"
          merge-multiple: true
          path: ids
      - name: Watch the wave's builds
        run: |
          set -eu
          ids=$(cat ids/build-id-*.txt | tr '\n' ' ')
          echo "watching builds: $ids"
          # the ids are one space-joined list
          # shellcheck disable=SC2086
          copr-cli watch-build $ids
          rc=0
          # shellcheck disable=SC2086
          for id in $ids; do
            status=$(copr-cli status "$id")
            if [ "$status" != succeeded ]; then
              echo "build $id: $status"
              rc=1
            fi
          done
          exit $rc

  submit3:
    name: "submit batch 3"
    needs: [manifest, wait2]
    if: always() && needs.manifest.outputs.batch3 != '' && (needs.wait2.result == 'success' || needs.wait2.result == 'skipped')
    strategy:
      matrix:
        pkg: ${{ fromJson(needs.manifest.outputs.batch3) }}
      fail-fast: false
    runs-on: ubuntu-latest
    timeout-minutes: 45
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Workspace safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Build the SRPM and submit to Copr
        env:
          NAME: ${{ matrix.pkg.name }}
        run: |
          set -euo pipefail
          spec="pkgs/$NAME/$NAME.spec"
          mkdir -p "_srpms/$NAME"
          spectool -g -C "_srpms/$NAME" "$spec"
          # spectool fetches only URL sources; local (non-URL) Source/Patch
          # entries (onlyoffice's metainfo, hyprland's macros.hyprland, …)
          # live in the package dir and go into the sourcedir verbatim
          cp -a "pkgs/$NAME/." "_srpms/$NAME/"
          srpm=$(rpmbuild -bs \
              --define "_sourcedir $PWD/_srpms/$NAME" \
              --define "_srcrpmdir $PWD/_srpms" \
              --define "_specdir $PWD/$(dirname "$spec")" \
              "$spec" | sed -n 's/^Wrote: //p')
          [ -n "$srpm" ] || { echo "rpmbuild -bs produced no SRPM"; exit 1; }
          copr-cli build --nowait "$COPR_PROJECT" "$srpm" | tee submit.log
          id=$(sed -n 's/^Created builds: //p' submit.log)
          [ -n "$id" ] || { echo "no build id in copr-cli output"; exit 1; }
          echo "$id" > "build-id-$NAME.txt"
      - name: Upload the build id
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: "submit-3-${{ matrix.pkg.name }}"
          path: build-id-*.txt
          if-no-files-found: error
          retention-days: 3

  wait3:
    name: "wait for batch 3"
    needs: [manifest, submit3]
    if: always() && needs.manifest.outputs.batch3 != '' && needs.submit3.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 330
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Download the wave's build ids
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: "submit-3-*"
          merge-multiple: true
          path: ids
      - name: Watch the wave's builds
        run: |
          set -eu
          ids=$(cat ids/build-id-*.txt | tr '\n' ' ')
          echo "watching builds: $ids"
          # the ids are one space-joined list
          # shellcheck disable=SC2086
          copr-cli watch-build $ids
          rc=0
          # shellcheck disable=SC2086
          for id in $ids; do
            status=$(copr-cli status "$id")
            if [ "$status" != succeeded ]; then
              echo "build $id: $status"
              rc=1
            fi
          done
          exit $rc

  # batch 4: the onlyoffice rewrap (363 MB payload). Copr has no Pages
  # budget — the R2 publish target is gone; the results land in the same
  # project repo.
  submit4:
    name: "submit batch 4 (onlyoffice)"
    needs: [manifest, wait3]
    if: always() && needs.manifest.outputs.batch4 != '' && (needs.wait3.result == 'success' || needs.wait3.result == 'skipped')
    strategy:
      matrix:
        pkg: ${{ fromJson(needs.manifest.outputs.batch4) }}
      fail-fast: false
    runs-on: ubuntu-latest
    timeout-minutes: 45
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Workspace safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Build the SRPM and submit to Copr
        env:
          NAME: ${{ matrix.pkg.name }}
        run: |
          set -euo pipefail
          spec="pkgs/$NAME/$NAME.spec"
          mkdir -p "_srpms/$NAME"
          spectool -g -C "_srpms/$NAME" "$spec"
          # spectool fetches only URL sources; local (non-URL) Source/Patch
          # entries (onlyoffice's metainfo, hyprland's macros.hyprland, …)
          # live in the package dir and go into the sourcedir verbatim
          cp -a "pkgs/$NAME/." "_srpms/$NAME/"
          srpm=$(rpmbuild -bs \
              --define "_sourcedir $PWD/_srpms/$NAME" \
              --define "_srcrpmdir $PWD/_srpms" \
              --define "_specdir $PWD/$(dirname "$spec")" \
              "$spec" | sed -n 's/^Wrote: //p')
          [ -n "$srpm" ] || { echo "rpmbuild -bs produced no SRPM"; exit 1; }
          copr-cli build --nowait "$COPR_PROJECT" "$srpm" | tee submit.log
          id=$(sed -n 's/^Created builds: //p' submit.log)
          [ -n "$id" ] || { echo "no build id in copr-cli output"; exit 1; }
          echo "$id" > "build-id-$NAME.txt"
      - name: Upload the build id
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: "submit-4-${{ matrix.pkg.name }}"
          path: build-id-*.txt
          if-no-files-found: error
          retention-days: 3

  wait4:
    name: "wait for batch 4 (onlyoffice)"
    needs: [manifest, submit4]
    if: always() && needs.manifest.outputs.batch4 != '' && needs.submit4.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 330
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Download the wave's build ids
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: "submit-4-*"
          merge-multiple: true
          path: ids
      - name: Watch the wave's builds
        run: |
          set -eu
          ids=$(cat ids/build-id-*.txt | tr '\n' ' ')
          echo "watching builds: $ids"
          # the ids are one space-joined list
          # shellcheck disable=SC2086
          copr-cli watch-build $ids
          rc=0
          # shellcheck disable=SC2086
          for id in $ids; do
            status=$(copr-cli status "$id")
            if [ "$status" != succeeded ]; then
              echo "build $id: $status"
              rc=1
            fi
          done
          exit $rc
  # batch 5: the texlive rolling groups — 39 Arch-style group specs +
  # texlive-meta, generated per tlnet snapshot by tools/texlive-splitter
  # (.github/workflows/texlive-update.yml rolls them biweekly). Each group
  # spec has no URL Sources (spectool fetches nothing); its %build wgets
  # the member tarballs from the dated snapshot — the documented
  # network-at-build exception.
  submit5:
    name: "submit batch 5 (texlive groups)"
    needs: [manifest, wait4]
    if: always() && needs.manifest.outputs.batch5 != '' && (needs.wait4.result == 'success' || needs.wait4.result == 'skipped')
    strategy:
      matrix:
        pkg: ${{ fromJson(needs.manifest.outputs.batch5) }}
      fail-fast: false
    runs-on: ubuntu-latest
    timeout-minutes: 45
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Workspace safe
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Build the SRPM and submit to Copr
        env:
          NAME: ${{ matrix.pkg.name }}
        run: |
          set -euo pipefail
          spec="pkgs/$NAME/$NAME.spec"
          mkdir -p "_srpms/$NAME"
          # no URL sources in the texlive group specs: the fetch happens in
          # %build from the dated snapshot (network is on in Copr builds)
          spectool -g -C "_srpms/$NAME" "$spec" || true
          srpm=$(rpmbuild -bs \
              --define "_sourcedir $PWD/_srpms/$NAME" \
              --define "_srcrpmdir $PWD/_srpms" \
              --define "_specdir $PWD/$(dirname "$spec")" \
              "$spec" | sed -n 's/^Wrote: //p')
          [ -n "$srpm" ] || { echo "rpmbuild -bs produced no SRPM"; exit 1; }
          copr-cli build --nowait "$COPR_PROJECT" "$srpm" | tee submit.log
          id=$(sed -n 's/^Created builds: //p' submit.log)
          [ -n "$id" ] || { echo "no build id in copr-cli output"; exit 1; }
          echo "$id" > "build-id-$NAME.txt"
      - name: Upload the build id
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: "submit-5-${{ matrix.pkg.name }}"
          path: build-id-*.txt
          if-no-files-found: error
          retention-days: 3

  wait5:
    name: "wait for batch 5 (texlive groups)"
    needs: [manifest, submit5]
    if: always() && needs.manifest.outputs.batch5 != '' && needs.submit5.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 330
    container:
      image: ghcr.io/${{ github.repository_owner }}/halcyon-builder:f44
    steps:
      - name: Copr credentials
        env:
          COPR_CLICONF: ${{ secrets.COPR_CLICONF }}
        run: |
          set -eu
          mkdir -p ~/.config
          printf '%s\n' "$COPR_CLICONF" > ~/.config/copr
          chmod 600 ~/.config/copr
      - name: Download the wave's build ids
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: "submit-5-*"
          merge-multiple: true
          path: ids
      - name: Watch the wave's builds
        run: |
          set -eu
          ids=$(cat ids/build-id-*.txt | tr '\n' ' ')
          echo "watching builds: $ids"
          # the ids are one space-joined list
          # shellcheck disable=SC2086
          copr-cli watch-build $ids
          rc=0
          # shellcheck disable=SC2086
          for id in $ids; do
            status=$(copr-cli status "$id")
            if [ "$status" != succeeded ]; then
              echo "build $id: $status"
              rc=1
            fi
          done
          exit $rc
